# Javascript Notes

## Intro

- HTML FILES ARE RENDERED FIRST, FOLLOWED BY CSS FILES AND THEN JS FILES GOES THROUGH THE BROWSER JAVASCRIPT ENGINES FOR EXAMPLE:
  - CHROME: V8
  - FIREFOX: SPIDERMONKEY
  - SAFARI: NITRO

## Xtics of programming languages

1. It must have a storage item such as variables to store values for later/ future use
2. It must have a loop system that allows repepitive jobs
3. It must have an execution item e.g functions that allows us to execute commands and decisions
4. It must have data-types or a data management system that allows us to distinguish between data of different types and use-case.

## Objects

Uses "key-value pair" or "property-value pair"
